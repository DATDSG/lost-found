# --- App ---
APP_NAME=lostfound-api
ENV=dev
PORT=8000
CORS_ORIGINS=*

# --- Auth ---
JWT_SECRET=changeme_super_secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# --- Database ---
DATABASE_URL=postgresql://lostfound:lostfound@database:5432/lostfound

# --- S3 / MinIO (Disabled for local development) ---
S3_ENDPOINT_URL=
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_BUCKET=media
S3_PRESIGN_EXPIRES=3600

# --- Redis / RQ ---
REDIS_URL=redis://redis:6379/0
RQ_DEFAULT_QUEUE=lostfound

# --- Admin bootstrap ---
ADMIN_EMAIL=admin@lostfound.local
ADMIN_PASSWORD=admin123

# --- Matching models & weights (removed - not defined in Settings class) ---
# These variables are not defined in the Settings class and cause validation errors
# The Settings class uses different variable names for matching weights