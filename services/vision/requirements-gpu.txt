# ================================================================
# Vision Service Requirements - GPU Version
# ================================================================
# WARNING: This will create a Docker image of 12-15GB+
# Only use if you have GPU support and NVIDIA Docker runtime
# ================================================================

# Include base requirements
-r requirements.txt

# PyTorch & TorchVision with CUDA support
# Install with: pip install torch==2.1.0 torchvision==0.16.0 --index-url https://download.pytorch.org/whl/cu118
torch==2.1.0
torchvision==0.16.0

# YOLO Object Detection
ultralytics==8.0.220  # YOLOv8

# OCR (Optical Character Recognition)
easyocr==1.7.1

# CLIP for advanced image understanding
git+https://github.com/openai/CLIP.git

# HuggingFace Transformers
transformers==4.37.2

# NSFW Detection
scikit-learn==1.3.2

# Optional: Additional GPU-accelerated libraries
# cupy-cuda11x==12.3.0  # GPU-accelerated NumPy
# opencv-contrib-python==4.8.1.78  # Full OpenCV with CUDA support

# ================================================================
# Installation Instructions for GPU:
# ================================================================
# 1. Build with GPU Dockerfile:
#    docker build -f Dockerfile.gpu -t vision-service:gpu .
#
# 2. Run with GPU support:
#    docker run --gpus all vision-service:gpu
#
# 3. Verify CUDA is available:
#    python -c "import torch; print(torch.cuda.is_available())"
#
# 4. Model files will be downloaded on first run (~2GB+)
# ================================================================
